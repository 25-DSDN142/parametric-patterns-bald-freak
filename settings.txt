You will need to fill out this file for hand in. 

The bellow is just an example, feel free to disregard this documents contents.


Your final settings.txt will look something like this when you are finished:

/////////////////////////////////

Wallpaper 1:

//your parameter variables go here!
let centre_diameter = 50 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 100 //this moves the flower on the x coordinate
let centre_y = 100 //this moves the flower on the y coordinate
let stem_width = 8 //this changes the size of the stem
let leaf_size = 50 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 2:

//your parameter variables go here!
let centre_diameter = 50 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 100 //this moves the flower on the x coordinate
let centre_y = 100 //this moves the flower on the y coordinate
let stem_width = 8 //this changes the size of the stem
let leaf_size = 50 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 100;
}

/////////////////////////////////

Wallpaper 3:

//your parameter variables go here!
let centre_diameter = 20 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 100 //this moves the flower on the x coordinate
let centre_y = 100 //this moves the flower on the y coordinate
let stem_width = 8 //this changes the size of the stem
let leaf_size = 0 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 100;
}

/////////////////////////////////

Wallpaper 4:

//your parameter variables go here!
let centre_diameter = 20 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 100 //this moves the flower on the x coordinate
let centre_y = 100 //this moves the flower on the y coordinate
let stem_width = 8 //this changes the size of the stem
let leaf_size = 30 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 100;
}

/////////////////////////////////

Wallpaper 5:

//your parameter variables go here!
let centre_diameter = 20 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 50 //this moves the flower on the x coordinate
let centre_y = 100 //this moves the flower on the y coordinate
let stem_width = 8 //this changes the size of the stem
let leaf_size = 30 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 100;
}

/////////////////////////////////

Wallpaper 6:

//your parameter variables go here!
let centre_diameter = 20 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 100 //this moves the flower on the x coordinate
let centre_y = 150 //this moves the flower on the y coordinate
let stem_width = 8 //this changes the size of the stem
let leaf_size = 30 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 100;
}

/////////////////////////////////

Wallpaper 7:

//your parameter variables go here!
let centre_diameter = 18 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 100 //this moves the flower on the x coordinate
let centre_y = 100 //this moves the flower on the y coordinate
let stem_width = 3 //this changes the size of the stem
let leaf_size = 30 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 100;
}

function wallpaper_background() {
  background(235, 233, 204); //beige colour
}

/////////////////////////////////

Wallpaper 8:

//your parameter variables go here!
let centre_diameter = 41 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 100 //this moves the flower on the x coordinate
let centre_y = 100 //this moves the flower on the y coordinate
let stem_width = 3 //this changes the size of the stem
let leaf_size = 30 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 100;
}

/////////////////////////////////

Wallpaper 9:

//your parameter variables go here!
let centre_diameter = 30 //this changes the size of the entire flower, and everything on the flower (petals, etc) is in relation to the size of the centre
let centre_x = 100 //this moves the flower on the x coordinate
let centre_y = 100 //this moves the flower on the y coordinate
let stem_width = 9 //this changes the size of the stem
let leaf_size = 60 //this changes the size of the leaves on the stem

function setup_wallpaper(pWallpaper) {
  //pWallpaper.output_mode(DEVELOP_GLYPH);
  pWallpaper.output_mode(GRID_WALLPAPER);
  
  pWallpaper.resolution(FIT_TO_SCREEN);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 200;
  pWallpaper.grid_settings.cell_height = 200;
  pWallpaper.grid_settings.row_offset  = 100;
}